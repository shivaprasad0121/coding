a = "AAACCCBAABB"
result = ""

count = 1
i = 0

while i < len(a) - 1:
    if a[i] == a[i + 1]:
        count += 1
    else:
        result += a[i] + str(count)
        count = 1
    i += 1

result += a[i] + str(count)

print(result)
